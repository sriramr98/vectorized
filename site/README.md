# Vectorized Field Guide

The interactive book for the Vectorized project. Content lives in `src/content/chapters`, and reusable exercise solutions live in `src/data/exercises.ts`.

```bash
npm install
npm run dev
npm run build
```
